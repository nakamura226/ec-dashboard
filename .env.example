# Copy to .env and adjust as needed. docker-compose reads this file automatically.

# MySQL root password used by both the db and web services.
DATABASE_PASSWORD=password

# Database name created inside the db container / used by Rails.
DATABASE_NAME=ec_dashboard_development

# Host port to expose the Rails server on (change if 3000 is already in use).
WEB_PORT=3000
